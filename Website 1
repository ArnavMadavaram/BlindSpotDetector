import streamlit as st

# Set page config
st.set_page_config(page_title="Dopamine Junkies - Blind Spot Detection", layout="wide")

# Custom CSS for dark futuristic theme
st.markdown("""
    <style>
        body {
            background-color: #111111;
            color: #FFFFFF;
        }
        .title {
            font-size: 60px;
            text-align: center;
            color: #00f0ff;
            font-weight: bold;
            padding: 20px 0;
        }
        .subtitle {
            font-size: 25px;
            text-align: center;
            color: #cccccc;
            margin-bottom: 40px;
        }
        .button-style button {
            background-color: transparent !important;
            color: #00f0ff !important;
            border: 2px solid #00f0ff !important;
            padding: 1rem 2rem !important;
            font-size: 18px !important;
            border-radius: 10px !important;
        }
        .button-style button:hover {
            background-color: #00f0ff !important;
            color: black !important;
        }
    </style>
""", unsafe_allow_html=True)

# Main title
st.markdown("<div class='title'>Dopamine Junkies 🚗💨</div>", unsafe_allow_html=True)
st.markdown("<div class='subtitle'>AI-Powered Blind Spot Detection in Foggy & Rainy Conditions</div>", unsafe_allow_html=True)

# Layout for hardware/software buttons
col1, col2 = st.columns(2)

with col1:
    st.markdown("<div class='button-style'>", unsafe_allow_html=True)
    if st.button("🔧 Hardware: Sensor Fusion"):
        st.switch_page("pages/1_Hardware.py")
    st.markdown("</div>", unsafe_allow_html=True)

with col2:
    st.markdown("<div class='button-style'>", unsafe_allow_html=True)
    if st.button("🧠 Software: Object Detection"):
        st.switch_page("pages/2_Software.py")
    st.markdown("</div>", unsafe_allow_html=True)
